import { Metadata } from "next";

export const metadata: Metadata = {
  title: 'Contato',
  description: 'Essa é a página contato'
}

export default function ConattoPage() {
  return (
    <main>
      <h2>Contato</h2>
    </main>
  );
}